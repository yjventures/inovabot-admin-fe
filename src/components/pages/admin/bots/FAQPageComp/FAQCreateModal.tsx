'use client'

import { useForm } from 'react-hook-form'

export default function FAQCreateModal() {
  const methods = useForm()
  const { handleSubmit } = methods
  return <div>FAQCreateModal</div>
}
